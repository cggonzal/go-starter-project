export PORT=8000
export RDS_USERNAME=cgg  # use psql to create locally if does not exist
export RDS_PASSWORD=mypass  # use psql to create locally if does not exist
export RDS_DB_NAME=mytestdb  # use psql to create locally if does not exist
export RDS_HOSTNAME=localhost  # use psql to create locally if does not exist
export RDS_PORT=5432  # default postgres port
