export PORT=8000
export PGUSER=cgg  # use psql to create locally if does not exist
export PGPASSWORD=mypass  # use psql to create locally if does not exist
export PGDATABASE=mytestdb  # use psql to create locally if does not exist
export PGHOST=localhost
export PGPORT=5432  # default postgres port
export SECRET_KEY=aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa  # only used for testing
